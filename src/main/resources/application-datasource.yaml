spring:
    config:
        activate:
            on-profile: local

    master-datasource:
        jdbcUrl: jdbc:mysql://127.0.0.1:3306/repill?characterEncoding=utf8&autoReconnect=true&useSSL=false
        username: root
        password: password
        maximumPoolSize: 5
        minimumIdle: 5
    jpa:
        database: mysql
        open-in-view: false
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                format_sql: true
                use_sql_comments: true
        show-sql: true